// En este ejemplo se comprueba si el compilador permite correctamente el uso de encadenado multples veces en una expresion, y que ademas permite de manera correcta 
//expresiones con multiples operandos


class Hola extends Pepito{
	static void m1(){
		((3+1).m1(this.var,new int[2].m2().variable.metodito(3,"cadena",Clase.algo(),this.variable)));
		abc = 32;
		if (1 || !this.var && 3 + 2 == 1 && 4 > var.m1() || !Clase.m3()) {
			a = 1;
		}
		{
			{
				{
					{
						{
							;
						}
					}
				}
			}
		}
	}
	
}